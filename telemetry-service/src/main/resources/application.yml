server:
  port: 8084

spring:
  application:
    name: telemetry-service
  datasource:
    url: jdbc:postgresql://postgres:5432/aegis_db
    username: aegis
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  kafka:
    bootstrap-servers: kafka:29092
    consumer:
      group-id: aegis-telemetry-group
      auto-offset-reset: earliest

logging:
  level:
    com.aegis: DEBUG
